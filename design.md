# bahadir.design 设计规范

本文档是 `bahadir.design` 的设计约束文件。后续新增页面、组件、动画、内容模块时，必须优先遵守这里的规范；只有当业务目标明确改变时，才允许更新本文件。

## 1. 品牌定位

### 核心气质

`bahadir.design` 是 AI 视觉工程专家 / 全栈开发者的个人作品集，不是传统简历页，也不是营销型 SaaS 官网。

设计关键词：

- 工业级 AI 视觉生产
- 编辑风作品集
- 黑白高对比
- 大字号、强秩序、少装饰
- 技术可信度 + 视觉冲击力
- 系统化、流水线化、可交付

### 设计目标

页面必须让访问者在 5 秒内理解三件事：

1. Bahadir 是做 AI 视觉生产流水线的人。
2. 他的能力同时覆盖视觉、产品工具、全栈开发和自动化。
3. 这是面向商业合作的高可信作品集。

### 禁止方向

- 不做可爱、卡通、软萌风格。
- 不做泛紫色 AI 科技渐变风。
- 不做模板化区块卡片堆叠。
- 不使用过多图标、插画、发光球、玻璃拟态装饰。
- 不把页面做成普通求职简历或课程销售页。

## 2. 色彩系统

### 主色 token

当前站点采用浅色首屏 + 深色主体 + 浅色联系区的节奏。

```css
:root {
  --bg: #f1f0ed;
  --bg-paper: #e8e6e1;
  --bg-dark: #0a0a0a;
  --ink: #1a1915;
  --text: #d4d0c8;
  --text-muted: #7a7870;
  --line-dark: rgba(212, 208, 200, 0.1);
  --line-light: rgba(26, 25, 21, 0.12);
}
```

### 使用规则

- 浅色区域背景使用 `#f1f0ed` 或 `#e8e6e1`，文字使用 `#1a1915`。
- 深色区域背景使用 `#0a0a0a`，主文字使用 `#d4d0c8`。
- 次级文字使用 `#7a7870` 或对应主文字的 50%-65% opacity。
- 分割线必须低对比：深色区 `rgba(212,208,200,0.08-0.12)`，浅色区 `rgba(26,25,21,0.08-0.15)`。
- 强调色尽量通过字号、字重、留白、运动和位置实现，不新增彩色强调。

### 色彩禁忌

- 不新增大面积品牌色。
- 不使用蓝紫渐变作为 AI 感表达。
- 不用纯白 `#ffffff` 作为大面积文字，避免破坏柔和纸感。
- 不用纯黑 `#000000` 替代 `#0a0a0a`，除非是媒体素材本身。

## 3. 字体与排版

### 字体栈

站点主字体由 `app/layout.tsx` 注入：

- 英文 / 数字：`DM Sans`
- 中文：`Noto Sans SC`
- 系统兜底：`Inter`, `-apple-system`, `PingFang SC`, `Microsoft YaHei`, sans-serif
- 数据编号、小标签、技能列表可使用 `monospace`

### 字重

- Hero / 一级标题：`900`
- 区块标题：`800-900`
- 卡片标题 / 重要段落：`600-700`
- 正文：`500`
- 辅助标签：`500-600`

### 字号层级

使用 `clamp()` 控制响应式字号，不用 `vw` 单独控制字体。

```css
/* Hero 英文 */
font-size: clamp(3rem, 9vw, 14rem);

/* Hero 中文 */
font-size: clamp(4.5rem, 14vw, 22rem);

/* 主区块标题 */
font-size: clamp(3rem, 10vw, 10rem);

/* 二级身份标题 */
font-size: clamp(2.5rem, 6vw, 7rem);

/* 大正文 */
font-size: clamp(15px, 1.4vw, 20px);

/* 正文 */
font-size: clamp(14px, 1.2vw, 17px);

/* 标签 / 导航 */
font-size: 10px-13px;
```

### 字距与行高

- Hero 大字允许负字距：`-0.03em` 到 `-0.04em`。
- 普通正文不使用负字距。
- 小标签使用大写感排版：`letter-spacing: 0.1em-0.15em`。
- Hero 大字行高：`0.85-1.0`。
- 区块标题行高：`1.05-1.15`。
- 正文行高：`1.6-1.8`。

### 文案排版规则

- 标题短、硬、具体，不写空泛口号。
- 正文强调“做成什么系统 / 提升什么效率 / 交付什么结果”。
- 中文正文避免过长，每段建议 1-3 句。
- 英文只用于品牌名、工具名、技术名、节奏标签，不把整站改成英文站。

## 4. 布局系统

### 页面结构

站点主节奏固定为：

1. 浅色 Hero：身份与商业合作入口。
2. 深色主体：服务、作品、关于。
3. 浅色联系区：表单、社交、备案。

不要随意打乱这个明暗节奏。新增重要区块优先放在深色主体内。

### 页面边距

- 桌面横向安全边距：`6vw`
- 移动横向安全边距：`5vw`
- 大区块纵向间距：`14vh-18vh`
- 复杂 sticky 区块允许使用 `100vh` 级滚动占位，但必须有移动端降级。

### 栅格

优先使用不对称栅格：

- Hero 底部：`1.3fr 0.7fr`
- About：`1.1fr 0.9fr`
- 服务卡片：`1fr 3fr`
- Footer：`1fr 1fr`

不要把所有内容默认居中或等宽卡片化。站点需要保持编辑风的不对称张力。

### 宽度控制

- 正文最大宽度：`380px-540px`
- 联系表单最大宽度：`800px`
- Footer 链接组最大宽度：`640px`
- 图片 / 视频默认占满内容列宽，避免小图漂浮。

## 5. 组件规范

### Header

- 位置：顶部绝对定位。
- 混合模式：`mix-blend-difference`。
- 左侧身份标签，右侧锚点导航。
- 导航文字保持小字号、monospace、大字距。
- Hover 使用上下滑动替换，不用下划线、背景块或彩色 hover。

### Hero

- 必须是第一屏主视觉，不加卡片容器。
- 背景为浅色纸感。
- 主视觉由超大中英文姓名构成。
- 上方是能力标签，下方是简介、行动按钮、可接项目时间。
- CTA 使用深色 pill 按钮，但页面中 pill 按钮数量要少。

### Services

- 服务区使用深色背景和大标题。
- 服务项采用 sticky 堆叠交互。
- 每个服务项包含编号、标题、描述、技术列表。
- 技术列表使用横向分割线和编号，不做彩色 tag 云。
- 移动端必须降级为单列、可自然滚动。

### Works

- 作品区强调媒体素材和结果，不做普通博客卡片。
- 项目内容必须来自 `data/projects.ts`，不要在组件里硬编码项目事实。
- 图片、视频应以连续作品流呈现，保持统一宽度和清晰可检视。
- 项目标题可使用换行制造节奏，但必须保持可读。
- 桌面端案例必须保持“左侧正文 sticky 固定、右侧作品自然滚动并驱动案例切换”的核心交互，左侧禁止增加独立滚动区域。
- 案例正文通过视口高度约束编号、标题与段间距，优先压缩装饰性大字和留白，不压缩正文可读字号；移动端保持自然文档流。

### About

- 左侧放身份判断和方法论，右侧放技能清单。
- 技能清单按能力类型分列，不做大量圆角标签。
- 技能文本可用 monospace 增强工程感。

### Contact

- 联系区回到浅色背景。
- 主标题居中并保持大字号。
- 表单使用底线输入，不使用传统白色 input 卡片。
- 提交按钮居中，保持深色 pill。
- Footer 保持低调，只放导航、社交、备案和版权。

## 6. 动效规范

### 动效原则

动效用于建立专业感和浏览节奏，不用于炫技。

允许的动效：

- Hero 字符逐字上滑出现。
- Section reveal：透明度 + Y 位移 + 轻微 scale。
- Sticky 叠层滚动。
- Header 链接上下滑动。
- Button hover 轻微 scale。
- 视频进入视口自动播放，悬停开声。

### 缓动曲线

优先使用以下曲线：

```css
/* 主要 reveal */
cubic-bezier(0.19, 1, 0.22, 1)

/* 链接滑动 */
cubic-bezier(0.51, 0.92, 0.24, 1.15)

/* 平滑按钮 hover */
cubic-bezier(0.16, 1, 0.3, 1)
```

### 时长

- 链接 hover：`0.4s`
- Button hover：`0.2s-0.3s`
- Reveal：`1.0s-1.2s`
- Overlay fade：`0.5s`
- 视频音量渐入：`150ms`
- 视频音量渐出：`250ms`

### 性能要求

- 大型滚动动画只改变 `transform` 和 `opacity`。
- 需要动画的元素可使用 `will-change`，但只对关键元素使用。
- 移动端关闭自定义 cursor。
- 移动端避免复杂 scroll-snap，优先自然滑动。

## 7. 响应式规范

### 断点

- 移动端主断点：`768px`
- 小屏 Header 断点：`640px`

### 移动端规则

- 页面横向边距改为 `5vw`。
- Hero padding 改为更紧凑的 `10vh 5vw 4vh`。
- 服务卡片改为单列。
- Works sticky 分屏降级为顺序垂直列表。
- About 改为单列。
- Footer 保持两列链接组，底部版权与备案可上下堆叠。
- 隐藏自定义 cursor。

### 不允许的问题

- 横向滚动。
- 大标题截断到不可读。
- Sticky 内容遮挡正文。
- 视频或图片溢出视口宽度。
- 按钮文字换行后高度跳动明显。

## 8. 媒体规范

### 图片

- 项目图片统一放在 `public/image/`。
- 优先使用 WebP。
- 作品图要能直接展示真实成果，不使用纯占位图作为最终交付。
- `next/image` 必须提供准确 `alt`。

### 视频

- 视频统一放在 `public/video/`。
- 默认静音、循环、`playsInline`。
- 进入视口播放，离开视口暂停。
- 悬停时才开声，离开后淡出并重新静音。
- 不在移动端强制开声。

### 外链素材

- 只有临时开发阶段允许使用 `placehold.co`。
- 正式上线前，项目缩略图必须替换为真实素材。

## 9. 交互与可访问性

### 鼠标与点击

- 桌面端保留自定义 cursor，但所有可点击元素仍必须有明确 hover 状态。
- 移动端使用系统 cursor / touch 行为。
- 可点击文字必须保持真实语义：链接用 `<a>` / `Link`，动作用 `<button>`。

### 表单

- 输入框 focus 必须有明显底线变化。
- 表单提交当前使用 `mailto:`，不引入后端前不要假装已发送成功。
- 微信复制必须有短暂反馈。

### 可访问性

- 交互元素必须有可读文本或 `aria-label`。
- 视频要提供 `aria-label`。
- 不要依赖颜色作为唯一状态表达。
- 新增动画应考虑 `prefers-reduced-motion`，至少不能阻断内容访问。

## 10. 代码落地规则

### 样式组织

当前项目混用 Tailwind、全局 CSS 和内联样式。新增代码遵守：

- 全局 token、响应式修正、通用动画放入 `app/globals.css`。
- 组件强相关、一次性的布局样式可以保留内联。
- 重复出现 3 次以上的样式，应提炼为 CSS class 或局部常量。
- 新增颜色必须先写入本文件和 `:root` token，再使用。

### 数据边界

- 项目事实写在 `data/projects.ts`。
- 页面组件只负责呈现，不硬编码项目背景、成果、链接。
- 联系方式如果变更，需要同步 README、页面和本规范中相关描述。

### 变更检查清单

每次改视觉或新增页面前，检查：

- 是否保持浅 / 深 / 浅的主节奏。
- 是否使用既有色彩 token。
- 是否没有新增模板化卡片堆叠。
- 是否桌面和移动端都无横向溢出。
- 是否动画只影响 `opacity` / `transform`。
- 是否真实素材可检视，不用模糊占位。
- 是否跑过至少一次 `npm run lint`。

## 11. 当前已知设计债

这些不是必须立即修，但后续改版应优先收敛：

- `ProjectCard.tsx`、`Footer.tsx`、`SkillTag.tsx` 保留了早期偏通用卡片风格，与首页当前编辑风不完全一致。
- 多处 token 仍以内联 hex 形式存在，应逐步迁移到 `:root`。
- `MenuOverlay.tsx` 当前未在首页主流程中使用，后续如果接入，需要统一导航语言和邮箱信息。
- `data/projects.ts` 中 AI 视频缩略图仍是占位图，正式上线应替换为真实画面。

