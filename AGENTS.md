# AGENTS.md

This repository uses `agent.md` as the project brain.

Before making any change in this repo, read and follow:

1. `agent.md`
2. `design.md`

For design, UI, layout, animation, responsive, media, or content changes, `design.md` is mandatory and must be treated as the source of truth unless the user explicitly overrides it.

